{
  "name": "dev-pipeline-frontend",
  "description": "Frontend development pipeline v5.3.0 — requirements-only backend handoff (feature brief, no API shapes; validate-handoff-brief leak guard; backend routes handoff to DISCOVER) + shared append-only cross-stack decision ledger (supersession + provenance) with CONTRACT-LANDED monitor, personas, TDD strict-edit guardrail, decomposition, cross-model review (Codex consult Hold+Expansion; configurable via DEV_PIPELINE_CROSS_MODEL), EARS-within-categorical-prefixes requirements, real-system verification (Two-Client Smoke, Live-BE Vitest, Curl Contract Gate), decision + requirement binding (verify-requirements-coverage, Phase Boundary Aggregation), MANIFEST Dependencies typed, visual QA, behavior-slice tasks, design mockup gates, design-stack primitives integration (PRODUCT.md, DESIGN.md, impeccable opt-in, GSAP motion routing)",
  "version": "5.4.0",
  "author": {
    "name": "Thoven",
    "email": "andres@thoven.co"
  },
  "license": "MIT",
  "keywords": ["development", "pipeline", "frontend", "skills"],
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/doc-staleness-nudge.js"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/session-context.js"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/check-update.js"
          }
        ]
      }
    ]
  }
}
